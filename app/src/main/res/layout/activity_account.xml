<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/banana_cream"
    android:orientation="vertical">

    <!-- Top Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:padding="12dp">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/back_label"
            android:src="@drawable/ic_manual_back" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/account_title"
            android:textColor="@color/banana_text_dark"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/library_language_pill"
            android:paddingHorizontal="14dp"
            android:paddingVertical="6dp"
            android:text="@string/language_current"
            android:textColor="@color/banana_green"
            android:textSize="11sp" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Profile / Guest Header Card -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/manual_card"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="24dp">

                <!-- Current Avatar Frame (Bigger size: 112dp x 112dp) -->
                <FrameLayout
                    android:id="@+id/profileAvatarFrame"
                    android:layout_width="120dp"
                    android:layout_height="120dp"
                    android:layout_gravity="center_horizontal"
                    android:clickable="true"
                    android:focusable="true"
                    android:foreground="?attr/selectableItemBackgroundBorderless">

                    <ImageView
                        android:id="@+id/profileAvatar"
                        android:layout_width="112dp"
                        android:layout_height="112dp"
                        android:layout_gravity="center"
                        android:background="@drawable/bg_avatar_item_selected"
                        android:clipToOutline="true"
                        android:contentDescription="@string/profile_label"
                        android:padding="10dp"
                        android:scaleType="fitCenter"
                        android:src="@drawable/avatar_cartoon_banana" />

                    <ImageButton
                        android:id="@+id/editAvatarButton"
                        android:layout_width="36dp"
                        android:layout_height="36dp"
                        android:layout_gravity="bottom|end"
                        android:background="@drawable/bg_avatar_edit"
                        android:contentDescription="@string/choose_avatar"
                        android:padding="9dp"
                        android:src="@drawable/ic_edit_avatar" />
                </FrameLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="14dp"
                    android:text="@string/guest_name"
                    android:textColor="@color/banana_text_dark"
                    android:textSize="22sp"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="@string/guest_description"
                    android:textColor="@color/banana_muted"
                    android:textSize="14sp" />
            </LinearLayout>


            <!-- Account / User Manual Section -->
            <TextView
                style="@style/ManualSection"
                android:layout_marginTop="22dp"
                android:text="@string/account_section" />

            <TextView
                android:id="@+id/manualRow"
                style="@style/ManualRow"
                android:drawableEnd="@drawable/ic_manual_chevron"
                android:drawablePadding="12dp"
                android:text="@string/user_manual" />

            <!-- Legal & Privacy Section -->
            <TextView
                style="@style/ManualSection"
                android:layout_marginTop="16dp"
                android:text="@string/legal_section" />

            <TextView
                android:id="@+id/termsRow"
                style="@style/ManualRow"
                android:drawableEnd="@drawable/ic_manual_chevron"
                android:drawablePadding="12dp"
                android:text="@string/terms_of_use" />

            <TextView
                android:id="@+id/privacyRow"
                style="@style/ManualRow"
                android:drawableEnd="@drawable/ic_manual_chevron"
                android:drawablePadding="12dp"
                android:text="@string/privacy_policy" />

            <TextView
                android:id="@+id/agreementRow"
                style="@style/ManualRow"
                android:drawableEnd="@drawable/ic_manual_chevron"
                android:drawablePadding="12dp"
                android:text="@string/user_agreement" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
